# User Value Inc. - 自社専用お問い合わせAPI セットアップガイド

Cloudflare Workers を使用して、**User Value Inc. 専用のメール送受信API（無料）** を立ち上げる手順です。

---

## 🎯 この仕組みで実現すること
- **完全オリジナル**: 他社フォームのロゴやリダイレクト画面は一切表示されません。
- **自社名義メール**: `User Value Inc. <noreply@uservalue.jp>` から届きます。
- **管理者通知 ＋ お客様への自動返信**: お問い合わせ内容の控えがお客様にも自動送信されます。
- **完全無料**: Cloudflare Workers（1日10万リクエスト無料）＋ Resend（月3,000通無料）

---

## 📋 セットアップ（所要時間: 約3分）

### ステップ1: Resend の無料アカウント作成・APIキー取得（1分）
メールを実際に送信するエンジンとして Resend を使用します。

1. **[Resend 公式サイト](https://resend.com/)** にアクセスしてサインアップ（無料）
2. 左メニュー **「API Keys」** ＞ **「Create API Key」** をクリック
3. 名前を `User Value Contact` 等にしてキーを作成
4. 発行された **`re_xxxx...`** のキーをコピーして控えておきます。

---

### ステップ2: Cloudflare Workers の作成（2分）

ブラウザの操作だけで完了します：

1. **[Cloudflare ダッシュボード](https://dash.cloudflare.com/)** にログイン
2. 左メニュー **「Compute (Workers & Pages)」** ＞ **「作成」** ＞ **「Workerの作成」** をクリック
3. 名前を `user-value-contact-api` などにして **「デプロイ」** をクリック
4. デプロイ後、画面右上の **「コードを編集」** をクリック
5. エディタ内の既存コードをすべて消去し、リポジトリ内の **[`worker/index.ts`](./worker/index.ts)** のコードを丸ごと貼り付けます。
6. 右上の **「保存してデプロイ」** をクリック！

---

### ステップ3: 環境変数（メール送信キー・通知先）の設定（1分）

1. 作成した Worker の管理画面に戻り、**「設定」** ＞ **「変数とシークレット」** を開きます。
2. **「変数を追加」** をクリックして以下の2つを設定します：

| 変数名 | タイプ | 値 | 説明 |
| :--- | :--- | :--- | :--- |
| **`RESEND_API_KEY`** | **シークレット (Encrypt)** | `re_xxxx...`（ステップ1で取得したキー） | メール送信用APIキー |
| **`ADMIN_EMAIL`** | **テキスト** | あなたの受信メールアドレス（例: `info@uservalue.jp` や Gmail等） | 問い合わせ通知を受信するアドレス |
| **`SENDER_EMAIL`** *(任意)* | **テキスト** | `User Value Inc. <onboarding@resend.dev>` または独自ドメイン送信アドレス | 送信元名義 |

3. **「デプロイ」** をクリックして保存します。

---

### ステップ4: 発行されたWorker URLの確認

Workerの画面上部に表示される URL（例: `https://user-value-contact-api.xxxx.workers.dev`）をコピーしてください。

※ 自社サブドメイン（例: `api.uservalue.jp`）を割り当てたい場合は、「設定」＞「ドメインとルート」から簡単に設定できます。

---

## 🚀 完了！

これで自社専用の送受信APIが世界最速のCDNエッジで稼働し、Webサイトから送信されたお問い合わせが確実にあなたのメールアドレスへ届くようになります！
